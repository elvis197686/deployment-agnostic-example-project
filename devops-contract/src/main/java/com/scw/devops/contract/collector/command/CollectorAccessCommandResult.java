package com.scw.devops.contract.collector.command;


public class CollectorAccessCommandResult {
	// All implementations must include a method getResult(), which must declare any checked exceptions.
	// It cannot be declared here as exceptions cannot be overridden.
	// example: List<Results> getResult() throws AnException, AnotherException;
}
