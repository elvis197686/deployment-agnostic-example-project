package com.scw.devops.contract.store.query.command;

public abstract class StoreQueryCommandResult {
	// All implementations must include a method processResult(), which must declare any checked exceptions.
	// It cannot be declared here as exceptions cannot be overridden.
	// example: List<Results> getResult() throws AnException, AnotherException;
}